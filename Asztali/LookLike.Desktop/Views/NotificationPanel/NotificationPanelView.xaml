<UserControl x:Class="LookLike.Desktop.Views.NotificationPanel.NotePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LookLike.Desktop.Views.NotificationPanel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 10">
            <TextBox Width="200" Margin="0 0 10 0" Text="{Binding NewNoteText, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="100" Margin="0 0 10 0" Text="{Binding NewNoteType, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Add Note" Command="{Binding AddNoteCommand}"
                    Background="#382d72"
                    Foreground="#e5ccf4"/>
        </StackPanel>

        <Button Content="Clear All Notes" Grid.Row="1" Command="{Binding ClearAllNotesCommand}" HorizontalAlignment="Left" Background="#382d72"
                Foreground="#e5ccf4"/>

        <ListView Grid.Row="2" ItemsSource="{Binding Notes}" Margin="0 10 0 0"/>
    </Grid>
</UserControl>