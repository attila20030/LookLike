/* browser.js v1.0.1 | @ajlkn | MIT licensed */
const browser = (function () {
    "use strict";
    let t = {
        name: null,
        version: null,
        os: null,
        osVersion: null,
        touch: null,
        mobile: null,
        _canUse: null,
        canUse: function (e) {
            t._canUse || (t._canUse = document.createElement("div"));
            let n = t._canUse.style,
                r = e.charAt(0).toUpperCase() + e.slice(1);
            return e in n || "Moz" + r in n || "Webkit" + r in n || "O" + r in n || "ms" + r in n;
        },
        init: function () {
            let e = navigator.userAgent,
                n = "other",
                r = 0,
                i = [
                    ["firefox", /Firefox\/([0-9\.]+)/],
                    ["bb", /BlackBerry.+Version\/([0-9\.]+)/],
                    ["bb", /BB[0-9]+.+Version\/([0-9\.]+)/],
                    ["opera", /OPR\/([0-9\.]+)/],
                    ["opera", /Opera\/([0-9\.]+)/],
                    ["edge", /Edge\/([0-9\.]+)/],
                    ["safari", /Version\/([0-9\.]+).+Safari/],
                    ["chrome", /Chrome\/([0-9\.]+)/],
                    ["ie", /MSIE ([0-9]+)/],
                    ["ie", /Trident\/.+rv:([0-9]+)/]
                ];
            for (let o = 0; o < i.length; o++)
                if (e.match(i[o][1])) {
                    n = i[o][0], r = parseFloat(RegExp.$1);
                    break;
                }
            let osList = [
                ["ios", /([0-9_]+) like Mac OS X/, e => e.replace("_", ".").replace("_", "")],
                ["ios", /CPU like Mac OS X/, e => 0],
                ["wp", /Windows Phone ([0-9\.]+)/, null],
                ["android", /Android ([0-9\.]+)/, null],
                ["mac", /Macintosh.+Mac OS X ([0-9_]+)/, e => e.replace("_", ".").replace("_", "")],
                ["windows", /Windows NT ([0-9\.]+)/, null],
                ["bb", /BlackBerry.+Version\/([0-9\.]+)/, null],
                ["bb", /BB[0-9]+.+Version\/([0-9\.]+)/, null],
                ["linux", /Linux/, null],
                ["bsd", /BSD/, null],
                ["unix", /X11/, null]
            ];
            let os = "other", osVersion = 0;
            for (let o = 0; o < osList.length; o++)
                if (e.match(osList[o][1])) {
                    os = osList[o][0], osVersion = parseFloat(osList[o][2] ? osList[o][2](RegExp.$1) : RegExp.$1);
                    break;
                }
            if (os === "mac" && "ontouchstart" in window &&
                (1024 === screen.width && 1366 === screen.height ||
                    834 === screen.width && 1112 === screen.height ||
                    810 === screen.width && 1080 === screen.height ||
                    768 === screen.width && 1024 === screen.height)) {
                os = "ios";
            }
            t.name = n;
            t.version = r;
            t.os = os;
            t.osVersion = osVersion;
            t.touch = os === "wp" ? navigator.msMaxTouchPoints > 0 : "ontouchstart" in window;
            t.mobile = ["wp", "android", "ios", "bb"].includes(t.os);
        }
    };
    t.init();
    return t;
})();

export default browser;
